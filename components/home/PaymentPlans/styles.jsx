import styled from 'styled-components'

export const ContentTech = styled.div`
    display: grid;
    gap: 10px;
    grid-template-columns: repeat(auto-fill,minmax(max(40px, 20% - 32px),1fr));
`
