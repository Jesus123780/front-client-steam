import styled from 'styled-components'

export const ContentVideo = styled.div`
    height: 95vh;
    display: flex;
    justify-content: center;
    & > video {
        object-fit: cover;
    }
`
